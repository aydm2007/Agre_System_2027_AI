مرحباً إبراهيم. 
بناءً على طلبك بأن يكون التعديل ضمن بيئة المشروع حصراً (Django Native) لتجنب السكريبتات المباشرة، قمت بالتالي:

1. **إصلاح نموذج (Model) `CropVariety`**: قمت بإضافة `null=True, blank=True` لحقل `crop` حتى يطابق السلوك المتوقع ويمنع خطأ `Not Null Violation`.
2. **إنشاء ملف الـ Migration**: تم إنشاء ملف `0061_alter_cropvariety_crop.py` بشكل برمجي ليقوم بتعديل الحقل.
3. **تطبيق Migration إعدادات النظام**: تأكدت من وجود ملف `0060_systemsettings_expense_auto_approve_limit.py` والذي سيقوم بإضافة عامود التكلفة المفقود.
4. **ترقيع مشكلة JSON Serializer**: تم حل مشكلة توقف سلسلة التدقيق والجنايات `_log_action` عبر استخدام `DjangoJSONEncoder` لمعالجة الـ `Decimal`.

**الخطوات المطلوبة منك الآن لبدء فحص (Harvest & Sales):**

بما أن الاتصال بقاعدة البيانات لديك كان معلقاً (Deadlock)، يرجى منك تنفيذ التالي في جهازك:
1. أعد تشغيل خدمة **PostgreSQL** محلياً لفك أي تعليق في الجلسات السابقة.
2. شغل الأمر التالي بشكل طبيعي داخل مجلد المشروع:
   ```bash
   python manage.py migrate core
   ```
3. أعد تشغيل خدمة **Redis** لديك لحل مشكلة `[WinError 10061]` الخاصة بروابط Celery و Kombu.

بمجرد الانتهاء، أعطني الإشارة لنكمل معاً محاكاة التقارير وحساب الزكاة لمحاصيل الحصاد! 🚀
