# Stabilize npm installs in CI and ephemeral containers.
# Hoisted install strategy is more reliable than link-heavy layouts for this repo.
install-strategy=hoisted
bin-links=true

# Keep CI noise down
fund=false
audit=false
