name: DEPRECATED LEGACY - v6-arabic-enterprise-static

on:
  workflow_dispatch:

jobs:
  verify-v6-static:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Run V6 static gates
        run: make verify-v6-static
