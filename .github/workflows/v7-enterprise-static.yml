name: DEPRECATED LEGACY - v7-enterprise-static

on:
  workflow_dispatch:

jobs:
  v7-static:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Run V7 static gates
        run: make verify-v7-static
      - name: Generate evidence
        run: make generate-evidence-v7
