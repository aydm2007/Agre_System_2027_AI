name: Backend PostgreSQL Tests

on:
  push:
    branches: ["main", "master", "develop"]
  pull_request:
    branches: ["main", "master"]

jobs:
  canonical:
    uses: ./.github/workflows/_shared-enterprise-ci.yml
